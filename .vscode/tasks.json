{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "SCIP: solve",
            "type": "shell",
            "command": "scip -f ${fileBasename} | awk '/primal solution (original space):/{flag=1; next} /Statistics/{flag=0} flag && !/^=+$/' | sed -n '/./,/^$/p'",
            "windows":{
                "command": "scip -f ${fileBasename} | findstr /r \"x#\\d*\"",
            },
            "group":{
                "kind": "build",
                "isDefault": true
            }
        }
	]
}

